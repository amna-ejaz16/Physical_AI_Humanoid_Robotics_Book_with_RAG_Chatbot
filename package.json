{
  "name": "physical-ai-humanoid-robotics-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Physical AI & Humanoid Robotics Book - Monorepo",
  "scripts": {
    "start": "cd docs && npm start",
    "build": "cd docs && npm run build",
    "serve": "cd docs && npm run serve",
    "install:all": "npm install && cd docs && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "dependencies": {
    "framer-motion": "^12.23.26"
  }
}
